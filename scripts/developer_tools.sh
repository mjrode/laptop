#!/bin/sh

# Spectacle allows you to organize your windows without using a mouse
brew cask list spectacle || brew cask install spectacle
# iTerm2 is a replacement for Terminal and the successor to iTerm.
brew cask list iterm2 || brew cask install iterm2
brew cask list tableplus || brew cask install tableplus
brew cask list docker || brew cask install docker
brew cask list apptivate || brew cask install apptivate
brew cask list flycut || brew cask install flycut
brew cask list google-chrome || brew cask install google-chrome
brew cask list visual-studio-code || brew cask install visual-studio-code
brew cask list postman || brew cask install postman
brew cask list evernote || brew cask install evernote
brew cask list fork || brew cask install fork
brew cask list atext || brew cask install atext
# Itsycal is a tiny calendar for your Mac’s menu bar.
brew cask list Itsycal || brew cask install Itsycal
